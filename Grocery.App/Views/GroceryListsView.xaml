<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Grocery.App.Views.GroceryListsView"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             xmlns:m="clr-namespace:Grocery.Core.Models;assembly=Grocery.Core"
             x:DataType="vm:GroceryListViewModel"
             Title="GroceryListsView">

	<ContentPage.ToolbarItems>
		<ToolbarItem Text="{Binding Client.Name}" Command="{Binding ShowBoughtProductsCommand}" />
	</ContentPage.ToolbarItems>

	<Shell.TitleView>
		<Grid>
			<Label Text="Boodschappenlijsten" FontSize="Large" HorizontalOptions="Center" VerticalOptions="Center" />
		</Grid>
	</Shell.TitleView>

	<Border Stroke="#C49B33"
            StrokeThickness="4"
            Padding="10"
            Margin="10"
            HorizontalOptions="Center">

		<CollectionView ItemsSource="{Binding GroceryLists}"
                        Margin="20"
                        HorizontalOptions="Center"
                        SelectionMode="None">
			<CollectionView.ItemsLayout>
				<LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
			</CollectionView.ItemsLayout>

			<CollectionView.Header>
				<Grid Padding="8,0">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="2*"/>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="24"/>
					</Grid.ColumnDefinitions>
					<Label Grid.Column="0" Text="Naam" FontAttributes="Bold"/>
					<Label Grid.Column="1" Text="Datum" FontAttributes="Bold" HorizontalTextAlignment="End"/>
					<Label Grid.Column="2" Text="Kleur" FontAttributes="Bold" HorizontalTextAlignment="Center"/>
				</Grid>
			</CollectionView.Header>

			<CollectionView.ItemTemplate>
				<DataTemplate x:DataType="m:GroceryList">
					<Grid Padding="8,6">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="2*"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="24"/>
						</Grid.ColumnDefinitions>

						<Label Grid.Column="0"
                               Text="{Binding Name}"
                               VerticalTextAlignment="Center"/>

						<Label Grid.Column="1"
                               Text="{Binding Date, StringFormat='{0:dd-MM-yyyy}'}"
                               HorizontalTextAlignment="End"
                               VerticalTextAlignment="Center"/>

						<Border Grid.Column="2"
                                WidthRequest="16"
                                HeightRequest="16"
                                HorizontalOptions="Center"
                                VerticalOptions="Center">
							<Border.Background>
								<SolidColorBrush Color="{Binding Color}" />
							</Border.Background>
						</Border>

						<Grid.GestureRecognizers>
							<TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ShowGroceryListItemsCommand}"
                                                  CommandParameter="{Binding .}" />
						</Grid.GestureRecognizers>
					</Grid>
				</DataTemplate>
			</CollectionView.ItemTemplate>
		</CollectionView>
	</Border>
</ContentPage>